[package]
name = "sense-input-win"
version.workspace = true
edition.workspace = true

[lib]

[dependencies]
sense-types = { workspace = true }
windows = { version = "0.58", features = [
  "Win32_Foundation",
  "Win32_UI_Input",
  "Win32_UI_WindowsAndMessaging",
  "Win32_System_Performance",
] }
